# The -O2 corpus semantic gate's pinned known failures (the golden list).
# Consumed as run_semantic.sh's 4th argument (pinned-knowns mode): green
# iff the failing set equals this list EXACTLY. Any difference is red —
# newly failing = REGRESSION, golden-listed now passing = IMPROVEMENT —
# and forces a deliberate update of this file + AGENTS.md (the
# re-baseline rule).
#
# State: 28/9 at the T4 merge (9b06689, bundle 76cb1c1b0ce17d16) — the
# conversion's inventoried reds. The corpora are 37 bins (T4 added the
# four indirect-call sources; NOTE the agent rebuilt /tmp/corpus* in
# place — against its isolation instruction; canaries verify the lanes).
byte_copy       # L3 SSE lane def-use (T5)
fizzbuzz_safe   # regressed at the T4 merge — re-attribute under the promoted model (T4b/inventory)
fn_escape       # NEW T4 source: the thunk path — conversion-correctness class (T4b)
fn_table_disp   # NEW T4 source: multi-target dispatch — conversion-correctness class (T4b)
jump_table_sw   # NEW T4 source: fails at -O2 only (T4b/inventory)
spill_many      # regressed at the T4 merge — re-attribute (T4b/inventory)
union_overlap   # L3 SSE lane def-use (T5)
va_arg_mixed    # L1 poison/alignment-guard class (unchanged by T4)
va_arg_vacopy   # the va_list state round-trip — T4's claimed improvement did NOT reproduce on the merged tree (T4b re-attributes)
