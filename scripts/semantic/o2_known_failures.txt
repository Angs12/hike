# The -O2 corpus semantic gate's pinned known failures (the golden list).
# Consumed as run_semantic.sh's 4th argument (pinned-knowns mode): green
# iff the failing set equals this list EXACTLY. Any difference is red —
# newly failing = REGRESSION, golden-listed now passing = IMPROVEMENT —
# and forces a deliberate update of this file + AGENTS.md (the
# re-baseline rule).
#
# 2026-09-10, the typed-model program's merged battery (T1+T2+T6,
# plugin bundle 29c2bb3d9e2e05a2): the list is EMPTY — the strict -O2
# semantic gate passes 33/33 on a fresh emission. The recorded six
# (byte_copy, fizzbuzz_safe, fptr_table, union_overlap, va_arg_mixed,
# va_arg_vacopy) do not reproduce even from the PRE-T1 tree (T1's
# verdict re-emitted from 775d616 unfixed: 33/33) — the -O2 corpus was
# rebuilt for the sp_reload lane after the 26/6 record, and the
# L1–L4 mechanisms are unobservable on the current binaries. The
# mechanism records survive in .scratch/o2-attribution/verdict.md and
# the typed-model tickets. The pin now guards an empty set: any new
# -O2 semantic failure is a REGRESSION by definition.
