_build/
src/_build/
src/*.plugin
src/node_modules/
*.ll
hike.plugin
gdb.txt
lm_test.ml
# zz_scratch_probe/ is the debug-reprobe harness home — VERSIONED since
# 2026-08-31 (the "1-byte stubs" era ended with the Item-1 recontruction).
# Only its dune _build is ignored.
zz_scratch_probe/_build/
