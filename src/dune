(library
  (name convlir)
  (public_name convlir)
  (preprocess (pps ppx_bap ))
  (libraries
    bap
    bap-core-theory
    bap-future
    bap-knowledge
    bap-main
    bitvec
    bitvec-order
    core_kernel
    monads
    re
    ogre))
