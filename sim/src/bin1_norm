
VAR = ADD(REG, INT)
REG = VAR
VAR = AND(REG, INT)
REG = VAR
VAR = ADD(REG, INT)
VAR = LOAD(REG, VAR, LE)
VAR = VAR
VAR = SUB(REG, INT)
REG = VAR
VAR = STORE(REG, REG, LE, VAR)
REG = VAR
IF INT THEN Direct call